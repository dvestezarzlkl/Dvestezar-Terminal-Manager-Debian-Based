{
  "version": 1,
  "commit_message": "Show centralized import processing state",
  "replacements": [
    {
      "path": "libs/app/settings_menu.py",
      "old": "    ) -> onSelReturn:\n        try:\n            report = apply_decoded_settings(\n                decoded,\n                allow_downgrade=allow_downgrade,",
      "new": "    ) -> onSelReturn:\n        print(\n            text_color(\n                \"Processing centralized settings import...\",\n                en_color.BRIGHT_CYAN,\n            ),\n            flush=True,\n        )\n        try:\n            report = apply_decoded_settings(\n                decoded,\n                allow_downgrade=allow_downgrade,",
      "expected": 1
    }
  ]
}
