{
  "version": 1,
  "commit_message": "SysApps 2.2.4: add startup Hub progress",
  "replacements": [
    {
      "path": "libs/app/hub/runtime.py",
      "old": "from typing import Optional",
      "new": "from typing import Callable, Optional",
      "expected": 1
    },
    {
      "path": "libs/app/hub/runtime.py",
      "old": "    def sync_all(self) -> HubSyncReport:\n        report = HubSyncReport()\n        settings = HubSettings.from_cfg()\n        started = perf_counter()\n        log.info(\n            \"SysApps Hub synchronization: start (%d provider(s))\",\n            len(self._providers),\n        )",
      "new": "    def sync_all(\n        self,\n        progress: Callable[[int, int, str], None] | None = None,\n    ) -> HubSyncReport:\n        report = HubSyncReport()\n        settings = HubSettings.from_cfg()\n        started = perf_counter()\n        providers = sorted(self._providers.items())\n        total_steps = len(providers) + 2\n\n        def emit_progress(step: int, label: str) -> None:\n            if progress is not None:\n                progress(step, total_steps, label)\n\n        log.info(\n            \"SysApps Hub synchronization: start (%d provider(s))\",\n            len(providers),\n        )",
      "expected": 1
    },
    {
      "path": "libs/app/hub/runtime.py",
      "old": "        try:\n            phase_started = perf_counter()\n            log.info(\"SysApps Hub database readiness check: start\")",
      "new": "        try:\n            emit_progress(1, \"core inventory\")\n            phase_started = perf_counter()\n            log.info(\"SysApps Hub database readiness check: start\")",
      "expected": 1
    },
    {
      "path": "libs/app/hub/runtime.py",
      "old": "        for key, registration in sorted(self._providers.items()):\n            try:\n                report.provider_counts[key] = self._sync_registration(",
      "new": "        for step, (key, registration) in enumerate(providers, start=2):\n            emit_progress(step, key.replace(\"_\", \" \"))\n            try:\n                report.provider_counts[key] = self._sync_registration(",
      "expected": 1
    },
    {
      "path": "libs/app/hub/runtime.py",
      "old": "        self.last_sync_at = datetime.now().astimezone()\n        phase_started = perf_counter()\n        log.info(\"SysApps Hub final status refresh: start\")",
      "new": "        self.last_sync_at = datetime.now().astimezone()\n        emit_progress(total_steps, \"finalization\")\n        phase_started = perf_counter()\n        log.info(\"SysApps Hub final status refresh: start\")",
      "expected": 1
    },
    {
      "path": "libs/app/hub/runtime.py",
      "old": "        print(\"SysApps Hub: synchronizing inventory...\")\n        report = self.sync_all()",
      "new": "        def print_progress(step: int, total: int, label: str) -> None:\n            print(f\"SysApps Hub: synchronization {step}/{total} - {label}...\")\n\n        report = self.sync_all(print_progress)",
      "expected": 1
    },
    {
      "path": "tests/test_hub_runtime.py",
      "old": "    HubState,\n    HubStatus,\n)",
      "new": "    HubState,\n    HubStatus,\n    HubSyncReport,\n)",
      "expected": 1
    },
    {
      "path": "tests/test_hub_runtime.py",
      "old": "    def test_remote_updates_are_applied_after_database_commit(self):",
      "new": "    def test_sync_all_reports_dynamic_progress(self):\n        runtime = HubRuntime()\n        runtime.register_provider(\n            \"node_red\",\n            lambda context: HubProviderSnapshot(\"node_red\", \"node_red_instances\", ()),\n        )\n        runtime.register_provider(\n            \"disks\",\n            lambda context: HubProviderSnapshot(\"disks\", \"disks\", ()),\n        )\n        progress = []\n\n        with patch(\"libs.app.hub.runtime.HubDatabase\", FakeDatabase), patch(\n            \"libs.app.hub.runtime.collect_host_snapshot\", return_value=self.host\n        ), patch(\n            \"libs.app.hub.runtime.configured_service_host\", return_value=\"host.example\"\n        ):\n            report = runtime.sync_all(lambda step, total, label: progress.append((step, total, label)))\n\n        self.assertTrue(report.core_synced)\n        self.assertEqual(\n            progress,\n            [\n                (1, 4, \"core inventory\"),\n                (2, 4, \"disks\"),\n                (3, 4, \"node red\"),\n                (4, 4, \"finalization\"),\n            ],\n        )\n\n    def test_startup_prints_progress_callback(self):\n        runtime = HubRuntime()\n        settings = HubSettings(\n            enabled=True,\n            host=\"db.example\",\n            port=3306,\n            user=\"sysapps\",\n            password=\"secret\",\n            database=\"sys_apps\",\n            prefix=\"sysapps_\",\n            connect_timeout=3,\n            auto_sync=True,\n        )\n        ready = HubStatus(HubState.READY, \"ready\", datetime.now().astimezone())\n\n        def fake_sync(progress=None):\n            progress(1, 2, \"core inventory\")\n            progress(2, 2, \"finalization\")\n            return HubSyncReport(core_synced=True)\n\n        with patch.object(runtime, \"refresh_status\", return_value=ready), patch(\n            \"libs.app.hub.runtime.HubSettings.from_cfg\", return_value=settings\n        ), patch.object(runtime, \"sync_all\", side_effect=fake_sync), patch(\n            \"builtins.print\"\n        ) as print_mock:\n            runtime.startup()\n\n        printed = [call.args[0] for call in print_mock.call_args_list]\n        self.assertIn(\"SysApps Hub: synchronization 1/2 - core inventory...\", printed)\n        self.assertIn(\"SysApps Hub: synchronization 2/2 - finalization...\", printed)\n\n    def test_remote_updates_are_applied_after_database_commit(self):",
      "expected": 1
    },
    {
      "path": "tests/test_startup_diagnostics.py",
      "old": "            \"Startup bootstrap runtime preflight: done\",\n            \"Startup splash delay: start\",\n            \"Startup splash delay: done\",\n            \"Startup bootstrap: ready for menu initialization\",",
      "new": "            \"Startup bootstrap runtime preflight: done\",\n            \"Startup bootstrap: ready for menu initialization\",",
      "expected": 1
    },
    {
      "path": "tests/test_startup_diagnostics.py",
      "old": "        ):\n            with self.subTest(marker=marker):\n                self.assertIn(marker, source)\n\n    def test_menu_boss_logs_startup_phases(self) -> None:",
      "new": "        ):\n            with self.subTest(marker=marker):\n                self.assertIn(marker, source)\n        self.assertNotIn(\"sleep(2)\", source)\n        self.assertNotIn(\"Startup splash delay\", source)\n\n    def test_menu_boss_logs_startup_phases(self) -> None:",
      "expected": 1
    },
    {
      "path": "libs/app/cfg.py",
      "old": "VERSION = \"2.2.3\"",
      "new": "VERSION = \"2.2.4\"",
      "expected": 1
    },
    {
      "path": "readme.md",
      "old": "v2.2.3",
      "new": "v2.2.4",
      "expected": 1
    },
    {
      "path": "changelog.md",
      "old": "# Changelog\n\n## v2.2.3",
      "new": "# Changelog\n\n## v2.2.4\n\n- PERF startup už neobsahuje historický pevný 2s splash delay; po základním bootstrapu pokračuje rovnou inicializací menu.\n- UX automatická SysApps Hub synchronizace při startu zobrazuje dynamický postup `x/y` přes základní inventář, každý registrovaný provider a finalizaci; detailní DB/collector časování zůstává v logu.\n- UPD hlavní verze aplikace na 2.2.4.\n\n## v2.2.3",
      "expected": 1
    },
    {
      "path": "AGENTS.md",
      "old": "- Startup bootstrap musí mít INFO milestone logy s elapsed časem přes hlavní blokující hranice: cfg/load, logger/jazyk/terminal/import hlavního menu, runtime preflight, explicitní splash delay, discovery/import dynamických `app_*`, update-check, central settings, Hub a handoff do `c_menu`. Logy mají diagnostikovat čekání bez vypisování credentials nebo obsahu konfigurace.",
      "new": "- Startup bootstrap musí mít INFO milestone logy s elapsed časem přes hlavní blokující hranice: cfg/load, logger/jazyk/terminal/import hlavního menu, runtime preflight, discovery/import dynamických `app_*`, update-check, central settings, Hub a handoff do `c_menu`. Startup nesmí obsahovat umělý pevný splash delay. Logy mají diagnostikovat čekání bez vypisování credentials nebo obsahu konfigurace.",
      "expected": 1
    },
    {
      "path": "libs/app/hub/AGENTS.md",
      "old": "- Full Hub synchronization must log INFO-level start/done milestones with elapsed time around database readiness, core collection, core DB sync, each provider collection/DB sync/remote apply, and final status refresh. Startup centralized-settings download/decode/policy/apply uses the same milestone principle. These logs are operational diagnostics for rare startup stalls and must never include credentials, URL paths containing possible tokens, package contents, or unredacted secrets.",
      "new": "- Full Hub synchronization must log INFO-level start/done milestones with elapsed time around database readiness, core collection, core DB sync, each provider collection/DB sync/remote apply, and final status refresh. Automatic startup synchronization must additionally expose coarse terminal progress `x/y` over core inventory, each registered provider and finalization; the total is dynamic and this UI progress must not alter provider isolation or provider APIs. Startup centralized-settings download/decode/policy/apply uses the same milestone principle. These logs are operational diagnostics for rare startup stalls and must never include credentials, URL paths containing possible tokens, package contents, or unredacted secrets.",
      "expected": 1
    }
  ]
}
