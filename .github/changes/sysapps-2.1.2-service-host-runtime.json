{
  "version": 1,
  "commit_message": "Add service host guard and normalization",
  "replacements": [
    {
      "path": "libs/app/cfg.py",
      "old": "VERSION = \"2.1.1\"",
      "new": "VERSION = \"2.1.2\"",
      "expected": 1
    },
    {
      "path": "libs/app/cfg.py",
      "old": "SERVER_URL:str            = \"moje.domena.fake\"              # jen doména/IP/popř sub path, bez portu",
      "new": "SERVER_URL:str            = \"\"                              # kompatibilní klíč: service host/FQDN/IP bez scheme, portu a path",
      "expected": 1
    },
    {
      "path": "libs/app/hub/runtime.py",
      "old": "from libs.JBLibs.helper import getLogger\n\nfrom .core_provider import collect_host_snapshot",
      "new": "from libs.JBLibs.helper import getLogger\nfrom libs.app.service_host import configured_service_host\n\nfrom .core_provider import collect_host_snapshot",
      "expected": 1
    },
    {
      "path": "libs/app/hub/runtime.py",
      "old": "    def refresh_status(self) -> HubStatus:\n        settings = HubSettings.from_cfg()\n        try:\n            self.status = HubDatabase(settings).check_status()",
      "new": "    def refresh_status(self) -> HubStatus:\n        settings = HubSettings.from_cfg()\n        if settings.enabled and not configured_service_host():\n            self.status = HubStatus(\n                HubState.NOT_CONFIGURED,\n                \"Service host / FQDN is not configured.\",\n                datetime.now().astimezone(),\n            )\n            return self.status\n        try:\n            self.status = HubDatabase(settings).check_status()",
      "expected": 1
    },
    {
      "path": "libs/app/hub/runtime.py",
      "old": "    def _ready_database(self) -> tuple[HubSettings, HubDatabase]:\n        settings = HubSettings.from_cfg()\n        status = HubDatabase(settings).check_status()",
      "new": "    def _ready_database(self) -> tuple[HubSettings, HubDatabase]:\n        settings = HubSettings.from_cfg()\n        if not configured_service_host():\n            self.status = HubStatus(\n                HubState.NOT_CONFIGURED,\n                \"Service host / FQDN is not configured.\",\n                datetime.now().astimezone(),\n            )\n            raise RuntimeError(self.status_text())\n        status = HubDatabase(settings).check_status()",
      "expected": 1
    },
    {
      "path": "libs/app/hub/core_provider.py",
      "old": "from libs.app import cfg\n\nfrom .models import HubAddress, HubHostSnapshot, HubService",
      "new": "from libs.app import cfg\nfrom libs.app.service_host import configured_service_host\n\nfrom .models import HubAddress, HubHostSnapshot, HubService",
      "expected": 1
    },
    {
      "path": "libs/app/hub/core_provider.py",
      "old": "def _service_url(port: Optional[int], use_https: bool) -> str:\n    host = _text(getattr(cfg, \"SERVER_URL\", \"\")).rstrip(\"/\")\n    if not host or port is None:\n        return \"\"\n    if \"://\" in host:\n        host = host.split(\"://\", 1)[1]\n    host = host.split(\"/\", 1)[0]\n    host = host.rsplit(\":\", 1)[0] if host.count(\":\") == 1 else host\n    scheme = \"https\" if use_https else \"http\"\n    return f\"{scheme}://{host}:{port}\"",
      "new": "def _service_url(port: Optional[int], use_https: bool) -> str:\n    host = configured_service_host()\n    if not host or port is None:\n        return \"\"\n    if \":\" in host and not host.startswith(\"[\"):\n        host = f\"[{host}]\"\n    scheme = \"https\" if use_https else \"http\"\n    return f\"{scheme}://{host}:{port}\"",
      "expected": 1
    },
    {
      "path": "libs/app/menus/app_20_node_red/handover_mail.py",
      "old": "from libs.app import cfg as app_cfg\nfrom libs.app import mail_hlp, user_contact",
      "new": "from libs.app import cfg as app_cfg\nfrom libs.app import mail_hlp, user_contact\nfrom libs.app.service_host import normalize_service_host",
      "expected": 1
    },
    {
      "path": "libs/app/menus/app_20_node_red/handover_mail.py",
      "old": "def build_instance_url(server_url: str, port: int, use_https: bool) -> str:\n    \"\"\"Build the externally presented instance URL from SERVER_URL and its port.\"\"\"\n    base = _text(server_url).rstrip(\"/\")\n    if not base:\n        return \"\"\n\n    default_scheme = \"https\" if use_https else \"http\"\n    if \"://\" not in base:\n        base = f\"{default_scheme}://{base}\"\n\n    parsed = urlsplit(base)\n    scheme = parsed.scheme or default_scheme\n    host = parsed.hostname\n    if not host:\n        # Handle a malformed historical SERVER_URL such as host:XXXX/path.\n        raw_netloc = parsed.netloc.split(\"@\", 1)[-1]\n        host = raw_netloc.split(\":\", 1)[0].strip(\"[]\")\n    if not host:\n        return \"\"\n    if \":\" in host and not host.startswith(\"[\"):\n        host = f\"[{host}]\"\n\n    path = parsed.path.rstrip(\"/\")\n    return urlunsplit((scheme, f\"{host}:{int(port)}\", path, \"\", \"\"))",
      "new": "def build_instance_url(server_url: str, port: int, use_https: bool) -> str:\n    \"\"\"Build the presented instance URL from the configured service host.\"\"\"\n    host = normalize_service_host(server_url)\n    if not host:\n        return \"\"\n    if \":\" in host and not host.startswith(\"[\"):\n        host = f\"[{host}]\"\n    scheme = \"https\" if use_https else \"http\"\n    return urlunsplit((scheme, f\"{host}:{int(port)}\", \"\", \"\", \"\"))",
      "expected": 1
    }
  ]
}
