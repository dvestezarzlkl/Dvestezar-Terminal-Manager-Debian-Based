{
  "version": 1,
  "commit_message": "Clarify settings import confirmation scope",
  "replacements": [
    {
      "path": "libs/app/settings_package.py",
      "old": "def registered_settings_sections() -> tuple[str, ...]:\n    return tuple(sorted(_SECTIONS))\n\n\ndef _b64_encode(value: bytes) -> str:",
      "new": "def registered_settings_sections() -> tuple[str, ...]:\n    return tuple(sorted(_SECTIONS))\n\n\ndef settings_section_label(section_key: str) -> str:\n    key = str(section_key or \"\")\n    handler = _SECTIONS.get(key)\n    return handler.label if handler is not None else key\n\n\ndef _b64_encode(value: bytes) -> str:",
      "expected": 1
    },
    {
      "path": "libs/app/settings_menu.py",
      "old": "    registered_settings_sections,\n    validate_settings_url,",
      "new": "    registered_settings_sections,\n    settings_section_label,\n    validate_settings_url,",
      "expected": 1
    },
    {
      "path": "libs/app/settings_menu.py",
      "old": "        remaining = set(decoded.sections).intersection(\n            registered_settings_sections()\n        ).difference(skip_sections)\n        if not remaining:\n            return True, False\n        current = int(cfg.SETTINGS_LAST_REVISION or 0)\n        downgrade = decoded.revision > 0 and decoded.revision < current\n        scope = \"remaining supported\" if skip_sections else \"supported\"\n        if downgrade:\n            ok = confirm(\n                f\"Package revision {decoded.revision} is older than local revision {current}. Apply this manual downgrade to the {scope} settings sections?\"\n            )\n            return ok, True\n        return confirm(f\"Apply the {scope} settings sections shown above?\"), False",
      "new": "        remaining = sorted(\n            set(decoded.sections)\n            .intersection(registered_settings_sections())\n            .difference(skip_sections)\n        )\n        if not remaining:\n            return True, False\n        labels = \", \".join(settings_section_label(key) for key in remaining)\n        qualifier = \"remaining \" if skip_sections else \"\"\n        noun = \"section\" if len(remaining) == 1 else \"sections\"\n        current = int(cfg.SETTINGS_LAST_REVISION or 0)\n        downgrade = decoded.revision > 0 and decoded.revision < current\n        if downgrade:\n            ok = confirm(\n                f\"Package revision {decoded.revision} is older than local revision {current}. Apply this manual downgrade to {qualifier}settings {noun}: {labels}?\"\n            )\n            return ok, True\n        return confirm(\n            f\"Apply {qualifier}settings {noun}: {labels}?\"\n        ), False",
      "expected": 1
    },
    {
      "path": "tests/test_settings_package.py",
      "old": "    preview_decoded_settings,\n    update_from_central_url,",
      "new": "    preview_decoded_settings,\n    settings_section_label,\n    update_from_central_url,",
      "expected": 1
    },
    {
      "path": "tests/test_settings_package.py",
      "old": "    def test_roundtrip_contains_hub_and_smtp_without_plaintext_secrets(self):",
      "new": "    def test_registered_section_labels_are_human_readable(self):\n        self.assertEqual(settings_section_label(\"hub\"), \"SysApps Hub\")\n        self.assertEqual(settings_section_label(\"smtp\"), \"SMTP\")\n        self.assertEqual(settings_section_label(\"future_section\"), \"future_section\")\n\n    def test_roundtrip_contains_hub_and_smtp_without_plaintext_secrets(self):",
      "expected": 1
    },
    {
      "path": "changelog.md",
      "old": "## v2.1.2\n\n- FIX SysApps Hub",
      "new": "## v2.1.2\n\n- FIX potvrzení po přeskočení konfliktní SMTP sekce vypisuje konkrétní zbývající sekce, například `SysApps Hub`, místo zavádějícího obecného dotazu\n- FIX SysApps Hub",
      "expected": 1
    }
  ]
}
