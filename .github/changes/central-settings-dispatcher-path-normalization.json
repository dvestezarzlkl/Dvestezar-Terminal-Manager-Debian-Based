{
  "version": 1,
  "commit_message": "Harden dispatcher path matching example",
  "replacements": [
    {
      "path": "docs/central-settings.md",
      "old": "$path = parse_url($_SERVER['REQUEST_URI'] ?? '/', PHP_URL_PATH);\n$path = '/' . trim(rawurldecode((string) $path), '/');",
      "new": "$path = parse_url($_SERVER['REQUEST_URI'] ?? '/', PHP_URL_PATH);\nif (!is_string($path)) {\n    http_response_code(404);\n    exit;\n}\n$path = '/' . trim($path, '/');",
      "expected": 1
    }
  ]
}
